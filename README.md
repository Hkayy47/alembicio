# alembicio
A zero-downtime migration orchestrator for vector databases ("Alembic for vectors"). Safely transition between embedding models with resumable backfills, dual-writes, and automated recall verification.
